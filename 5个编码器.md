MCU控制线：
  □ Pin26 → HC4067_1 S0 + HC4067_2 S0（并联）
  □ Pin27 → HC4067_1 S1 + HC4067_2 S1（并联）
  □ Pin28 → HC4067_1 S2 + HC4067_2 S2（并联）
  □ Pin29 → HC4067_1 S3 + HC4067_2 S3（并联）
  □ Pin11 ← HC4067_1 SIG
  □ Pin12 ← HC4067_2 SIG

HC4067电源（两片相同）：
  □ UCC  → 3.3V
  □ GND  → GND
  □ EN   → GND（常开）
  □ 100nF电容 UCC→GND 紧贴模块

HC4067_1通道（C0~C15 共16个10kΩ）：
  □ C0  ──┬── ENC1_SW  [10kΩ]→3.3V  另脚→GND
  □ C1  ──┬── ENC2_SW  [10kΩ]→3.3V  另脚→GND
  □ C2  ──┬── ENC3_SW  [10kΩ]→3.3V  另脚→GND
  □ C3  ──┬── ENC4_SW  [10kΩ]→3.3V  另脚→GND
  □ C4  ──┬── ENC5_SW  [10kΩ]→3.3V  另脚→GND
  □ C5  ──┬── 按键1    [10kΩ]→3.3V  另脚→GND
  □ C6  ──┬── 按键2    [10kΩ]→3.3V  另脚→GND
  □ C7  ──┬── 按键3    [10kΩ]→3.3V  另脚→GND
  □ C8  ──┬── 按键4    [10kΩ]→3.3V  另脚→GND
  □ C9  ──┬── 按键5    [10kΩ]→3.3V  另脚→GND
  □ C10 ──┬── 按键6    [10kΩ]→3.3V  另脚→GND
  □ C11 ──┬── 按键7    [10kΩ]→3.3V  另脚→GND
  □ C12 ──┬── 按键8    [10kΩ]→3.3V  另脚→GND
  □ C13 ──┬── 按键9    [10kΩ]→3.3V  另脚→GND
  □ C14 ──┬── 按键10   [10kΩ]→3.3V  另脚→GND
  □ C15 ──┬── 按键11   [10kΩ]→3.3V  另脚→GND

HC4067_2通道（C0~C8 共9个10kΩ）：
  □ C0  ──┬── 按键12   [10kΩ]→3.3V  另脚→GND
  □ C1  ──┬── 按键13   [10kΩ]→3.3V  另脚→GND
  □ C2  ──┬── 按键14   [10kΩ]→3.3V  另脚→GND
  □ C3  ──┬── 按键15   [10kΩ]→3.3V  另脚→GND
  □ C4  ──┬── 按键16   [10kΩ]→3.3V  另脚→GND
  □ C5  ──┬── 按键17   [10kΩ]→3.3V  另脚→GND
  □ C6  ──┬── 按键18   [10kΩ]→3.3V  另脚→GND
  □ C7  ──┬── 按键19   [10kΩ]→3.3V  另脚→GND
  □ C8  ──┬── 按键20   [10kΩ]→3.3V  另脚→GND
  □ C9~C15 空置不接